/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Tomado API
 * 포모도로 타이머 + 투두리스트 + 데일리로그 + 회고록을 통합한 생산성 기록 서비스

## 인증
보호된 엔드포인트는 `Authorization: Bearer {access_token}` 헤더 또는 `access_token` 쿠키가 필요합니다.
Access Token 만료 시 `/auth/refresh`로 재발급하세요.

## 삭제 정책
완료된 투두(`completed_at IS NOT NULL`)는 삭제 불가 — 기록 보존 원칙.

 * OpenAPI spec version: 1.0.0
 */
import type { RetroLogContent } from './retroLogContent';
import type { RetroLogDraftContent } from './retroLogDraftContent';
import type { RetroLogTemplateType } from './retroLogTemplateType';

export interface RetroLog {
    id?: string;
    user_id?: string;
    daily_log_id?: string;
    retro_date?: string;
    template_type?: RetroLogTemplateType;
    /** template_type(Tech|Decision|Communication|Emotion)에 따라 아래 oneOf 중 하나의 객체 형태입니다.
  각 필드 값은 문자열이면 빈 문자열("")도 허용합니다(요청 본문 `content`와 동일한 규칙).
   */
    content?: RetroLogContent;
    is_dirty?: boolean;
    /** @nullable */
    draft_content?: RetroLogDraftContent;
    created_at?: string;
    updated_at?: string;
}
